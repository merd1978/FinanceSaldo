<UserControl x:Class="FinanceSaldo.View.CompanyView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ignore="http://www.galasoft.ch/ignore"
        mc:Ignorable="d ignore"
        DataContext="{Binding Main, Source={StaticResource Locator}}" d:DesignWidth="200.881" d:DesignHeight="348.358">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <TextBlock TextWrapping="Wrap"
                   TextAlignment="Center" Text="List of Companies" FontWeight="Bold" FontSize="16"/>
        <DataGrid x:Name="dgCompany" Grid.Row="1" ItemsSource="{Binding Company}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Предприятие" Binding="{Binding Name}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="btnloadinovice" Content="New company" 
                Grid.Row="2"/>
    </Grid>
</UserControl>
