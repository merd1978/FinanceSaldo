<UserControl x:Class="FinanceSaldo.View.InvoiceView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ignore="http://www.galasoft.ch/ignore"
        xmlns:view="clr-namespace:FinanceSaldo.View"
        mc:Ignorable="d ignore"
        d:DesignWidth="743.881" d:DesignHeight="348.358">

    <StackPanel Orientation="Vertical" VerticalAlignment="Top">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock TextWrapping="Wrap"
                           Text="Список накладных" FontWeight="Bold" FontSize="16" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding Company.Name}" FontWeight="Bold" FontSize="16"/>
             </StackPanel>
            <DataGrid x:Name="DgInvoice" Grid.Row="1" ItemsSource="{Binding Invoice}" AutoGenerateColumns="False" CanUserAddRows="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="№ Накладной" Width="175" 
                                        Binding="{Binding Name}">
                    </DataGridTextColumn>
                    <DataGridTemplateColumn Header="Дата">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Date, StringFormat=d}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <DatePicker  SelectedDate="{Binding Path=Date}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Дебит" 
                                        Binding="{Binding Debit}">
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Кредит" 
                                        Binding="{Binding Credit}">
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
        <StackPanel Margin="0 10 0 10" HorizontalAlignment="Center" Orientation="Horizontal">
            <Button Content="Сохранить" Margin="10 0 10 0" Command="{Binding UpdateCompanyCommand}"/>
            <Button Content="Отмена" Margin="10 0 10 0" Command="{Binding CloseTabCommand}"/>
        </StackPanel>
    </StackPanel>
</UserControl>
